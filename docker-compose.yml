services:
  backend:
    image: ghcr.io/ahricool/cool:latest
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - /data/cool:/app/.database
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped
